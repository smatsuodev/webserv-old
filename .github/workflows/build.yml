name: build
on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
jobs:
  build:
    runs-on: macos-latest
    strategy:
      matrix:
        compiler: [g++, clang++]
    steps:
      - uses: actions/checkout@v3
      - name: Install gcc
        if: ${{ matrix.compiler == 'g++' }}
        run: brew install gcc
      - name: Configure CMake
        run: CXX=${{ matrix.compiler }} cmake -B build
      - name: Build
        run: cmake --build build